spring:
  application:
    name: wongoo3

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: ${NAVER_OAUTH_CLIENT_ID:YymH4a_h3ZNvKYkzTkCX}
            client-secret: ${NAVER_OAUTH_CLIENT_SECRET:UDiHWmw8sS}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope: name, email, profile_image, nickname, mobile
            client-name: Naver
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  datasource:
    url: jdbc:mysql://localhost:3306/wongoo3?serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 15730254791!a

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    operations-sorter: method
    tags-sorter: alpha

jwt:
  secret: wongoo3-secret-key-heo-yoo-jeong-hong-won-ki-since-2023-02-04
  access-expiration-ms: 1800000  # 30 minutes
  refresh-expiration-ms: 1209600000 # 14일

app:
  frontend-url: ${FRONTEND_URL:http://localhost:5173}

naver:
  oauth:
    client-id: ${NAVER_OAUTH_CLIENT_ID:YymH4a_h3ZNvKYkzTkCX}
    client-secret: ${NAVER_OAUTH_CLIENT_SECRET:UDiHWmw8sS}
    auth-uri: ${NAVER_OAUTH_AUTH_URI:https://nid.naver.com/oauth2.0/authorize}
    redirect-uri: ${NAVER_OAUTH_REDIRECT_URI:http://localhost:8080/api/auth/naver/callback}
    token-uri: ${NAVER_OAUTH_TOKEN_URI:https://nid.naver.com/oauth2.0/token}
    user-info-uri: ${NAVER_OAUTH_USER_INFO_URI:https://openapi.naver.com/v1/nid/me}